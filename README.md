# display-mdb-connections
Simple VB script to display who is connected to certain .mdb and where. It also supports .mdb with password.

## usage

```
	cscript.exe .\display.vbs '<c:\full\path\accessDb.mdb>' ['password-for-db']
```

